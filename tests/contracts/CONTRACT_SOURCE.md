# Canonical contract snapshot

The conformance artifacts in this directory are a pinned, test-only snapshot
from `pancakebaker/dbap-integration-contracts` at commit
`6c89fb85d96110b881ff789470e3d7acfd5331e6`.

They are used to verify that Live Feed accepts the canonical event fixtures.
They do not create a runtime dependency or replace the service-local parser.
